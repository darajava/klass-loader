import React from 'react';

const Component = (props) => {

  return (
      <div klass={['but', 'ton'].map((item) => {
        return item;
      }).join('')}>

      </div>
  );
        
}

export default Component;
 